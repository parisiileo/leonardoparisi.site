/**
 * X/Twitter reads its own tag when one exists; pointing it at the same card
 * keeps a single design to maintain.
 */
export {
  alt,
  contentType,
  default,
  generateStaticParams,
  size,
} from "./opengraph-image";
